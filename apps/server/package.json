{
  "name": "@tap-to-trade/server",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit",
    "start": "bun dist/index.js"
  },
  "dependencies": {
    "@privy-io/server-auth": "^1.18.0",
    "@tap-to-trade/shared": "workspace:*",
    "hono": "^4.6.0",
    "postgres": "^3.4.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.0",
    "bun-types": "^1.3.10",
    "typescript": "^5.7.0"
  }
}
